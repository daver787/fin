import { Panel, PlaceholderBody } from "./Panel";

// Placeholder for the positions table (owned by fin-vc7p).
export function PositionsTable() {
  return (
    <Panel title="Positions">
      <PlaceholderBody note="No open positions" />
    </Panel>
  );
}
