import { Panel, PlaceholderBody } from "./Panel";

// Placeholder for the portfolio heatmap + P&L chart (owned by fin-vc7p).
export function PortfolioViz() {
  return (
    <Panel title="Portfolio">
      <PlaceholderBody note="Heatmap + P&L chart" />
    </Panel>
  );
}
